const { EmbedBuilder } = require('@fluxerjs/core');

module.exports = {
    config: {
        name: "",
        usage: true,
        cooldown: 5000,
        available: true,
        permissions: {},
        aliases: []
    },
    run: async (client, message, args, db) => {

        new Paginator([], { timeout: 5 * 2e4, user: message.author.id, client: client })
            .add([embed, embed2])
            .start(message.channel);
    },
};
 